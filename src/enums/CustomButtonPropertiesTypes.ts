export enum CustomButtonTypes {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}

export enum CustomButtonColors {
  DEFAULT = "default",
  PRIMARY = "primary",
  SECONDARY = "secondary",
  SUCCESS = "success",
  WARNING = "warning",
  DANGER = "danger",
}

export enum CustomButtonVariants {
  SOLID = "solid",
  BORDERED = "bordered",
  LIGHT = "light",
  FLAT = "flat",
  FADED = "faded",
  SHADOW = "shadow",
  GHOST = "ghost",
}
